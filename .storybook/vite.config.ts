import { defineConfig } from "vite";

// The application Vite config contains the Vinext + Cloudflare RSC pipeline.
// Storybook needs a browser-only config; Next.js support is added by its framework preset.
export default defineConfig({});
